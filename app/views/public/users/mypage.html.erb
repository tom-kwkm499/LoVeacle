<div class="container">
  <h1>マイページ</h1>
  <div class="row">
    <div class="col-4">
      <div class="row">
        <%= link_to '新規車両登録', new_user_car_path(user_id: @user.id), class: "btn btn-success btn-block" %>
      </div>
      <div class="row mt-2">
        <% if @cars.any? %>
          <div class="col-4">
            <% @cars.each do |car| %>
              <div class="card p-1 card-hover">
                <p class="text-center">
                  <%= car.manufacturer %><br>
                  <strong><%= car.car_name %></strong><br>
                  <%= car.model_code %><br>
                  <%= car.trim_level %>
                </p>
                <!-- <button type="button" class="btn btn-primary"> -->
                  <%#= link_to post_path(item.id) do %>
                <!-- </button> -->
              </div>
            <% end %>
          </div>
        <% else %>
          <p>車両を登録して投稿を始めてみましょう</p>
        <% end %>
      </div>
    </div>
  </div>
</div>